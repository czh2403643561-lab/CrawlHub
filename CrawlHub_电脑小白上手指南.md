# CrawlHub 电脑小白上手指南

## 一、你需要准备什么

- Windows 电脑
- Chrome 或 Microsoft Edge 浏览器
- CrawlHub 压缩包
- 需要采集数据的网页账号和页面

CrawlHub 是浏览器扩展，只在本地处理页面数据，不会上传账号信息，也不会自动登录。

## 二、第一次安装

### 1. 解压压缩包

右键点击 `CrawlHub_可运行版.zip`，选择“全部解压缩”。

记住解压后的文件夹位置，不要删除或移动这个文件夹。

### 2. 打开浏览器扩展页面

在 Chrome 地址栏输入：

`chrome://extensions`

在 Edge 地址栏输入：

`edge://extensions`

打开页面右上角的“开发者模式”。

### 3. 加载 CrawlHub

点击“加载已解压的扩展程序”，选择刚才解压的文件夹。

请选择能够直接看到 `manifest.json` 的那一层文件夹。

看到“CrawlHub - AI 页面结构分析助手”后，安装就完成了。

建议点击扩展右侧的图钉，把 CrawlHub 固定在浏览器工具栏。

## 三、打开 CrawlHub

1. 在浏览器中打开要采集的网页。
2. 等网页内容和表格加载完成。
3. 点击浏览器工具栏中的 CrawlHub 图标。
4. 页面右侧会出现 CrawlHub 面板。

## 四、最简单的采集流程

### 采集普通表格或商品榜

1. 打开 CrawlHub 面板。
2. 切换到“数据采集”模式。
3. 先查看字段预览，确认排名、商品名称、图片、价格等字段有内容。
4. 点击“采集当前页”。
5. 等待提示“第 X 页完成”。

如果有下一页：

1. 在原网页点击下一页。
2. 等待新页面内容加载完成。
3. 回到 CrawlHub，再点击“采集当前页”。

这样会继续追加数据，不会覆盖前面已经采集的页面。

## 五、页面分析和元素采样

普通表格通常可以直接采集，不需要先分析。

如果字段识别不完整：

1. 切换到“页面分析”模式。
2. 点击“开始元素采样”。
3. 点击网页中需要确认的表头、商品名称或图片区域。
4. 需要滚动网页时，先点击“暂停采样”。
5. 滚动到目标位置后点击“继续采样”。
6. 完成后再回到“数据采集”模式。

按 `Esc` 可以取消当前这轮元素采样。

元素采样主要用于确认页面结构，不会自动修改网页内容。

## 六、导出数据

1. 采集完成后，点击面板顶部的齿轮。
2. 第一次导出时选择默认导出根目录。
3. 点击“导出项目”。

导出后的目录大致如下：

```text
导出根目录/
└── 类目/
    └── 榜单/
        ├── metadata.json
        ├── products.json
        ├── products.csv
        └── products.xlsx
```

如果同一个类目和榜单已经导出过，CrawlHub 会先询问是否覆盖，请根据需要选择。

## 七、短视频榜说明

短视频榜会读取页面当前已经显示的视频封面，保存到商品的 `videos` 字段中。

CrawlHub 不会：

- 点击视频
- 打开播放器
- 获取视频播放地址
- 下载视频

如果页面还没有加载视频封面，请先等待页面加载完成，再重新采集当前页。

## 八、遇到问题怎么办

### 面板提示插件刚刚更新或连接失效

这是浏览器扩展重新加载后的常见提示：

1. 先关闭 CrawlHub 面板。
2. 回到目标网页并刷新网页。
3. 再点击工具栏中的 CrawlHub 图标。
4. 如果面板中有“重新连接页面”，点击它。

字段模板、导出目录设置和已经保存的数据不会因为重新连接而主动删除。

### 字段没有识别出来

确认：

- 表头和商品行已经加载完成。
- 当前页面确实是数据列表页。
- 没有停留在加载动画或空白页。

仍然无法识别时，可以使用“页面分析”和“元素采样”确认目标区域。

### 采集数量比页面少

CrawlHub 默认采集当前页面已经加载并可见的数据。请先滚动或翻页，让网页加载更多内容，再点击“采集当前页”。

### 想重新安装

在扩展页面删除旧的 CrawlHub，再使用“加载已解压的扩展程序”选择同一个解压文件夹即可。

## 九、重要提醒

- 不要删除正在使用的解压文件夹。
- 更新版本时，先关闭 CrawlHub 面板，再在扩展页面点击“重新加载”。
- 不要把包含个人账号、Cookie 或其他隐私文件的文件夹一起分享。
- 导出的数据默认保存在你自己选择的本地目录中。
