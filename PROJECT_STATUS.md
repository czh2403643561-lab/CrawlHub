# Project Status

## 已完成

- 项目基础环境初始化完成
- 项目定义已调整为：CrawlHub - AI 页面结构分析助手
- 第一阶段方向已调整为通用网页取证包生成器，而非页面采集器
- 已完成 Chrome Extension Manifest V3 基础结构
- 已完成页面信息、DOM、文本、图片、列表/表格、JSON 和网络线索分析
- 已完成页面元素选择分析，支持标签、class、selector、XPath、文本和父级结构
- 已完成本地 `analysis.json` 报告生成与下载
- 第二阶段已完成：元素分析增加兄弟节点、附近结构和通用字段类型推测
- 第二阶段已完成：表格增加列名、样例值和行记录关系分析
- 第二阶段已完成：增加本地 fetch/XHR 后续请求监听，仅记录脱敏 URL 和响应元信息
- 交互优化已完成：分析面板固定悬浮显示，支持最小化和关闭
- 交互优化已完成：元素选择改为持续采样，可连续保存多个元素并统一生成报告
- 项目方向已调整为双模式：页面分析模式 + 数据采集模式
- 数据采集模式第一版已完成：根据当前页表格表头、行和列关系生成通用字段模板
- 数据采集模式第一版已完成：可提取当前已加载的表格数据，并预览商品/条目数量、字段和示例行
- 数据采集模式第一版已完成：支持标准列表作为表格未识别时的回退，提取列表项文本和图片
- 数据采集模式输出已完成：支持字段化结果表格预览、当前页 CSV/JSON 导出
- 数据采集模式输出已完成：支持仅保存字段映射的本地模板基础能力
- 数据质量优化已完成：商品单元格中的标题、价格范围、商品评分和评价数量可拆分为独立字段
- 数据质量优化已完成：商品评分和评价数量输出为数值，CSV 与 JSON 按固定字段模板输出，并保留列关系元数据
- 数据采集字段已完善：通过通用表头和同一行单元格关系提取排名，排名变化值不会混入排名字段
- 排名解析已修复：只提取排名主数字，忽略 `↑数字`、`↓数字` 和 `-` 趋势内容
- 数据采集结构已升级：顶部已采样类目写入 metadata，包含类目全称、简称、采集时间、页面名称和链接
- 排名字段已升级：按排名列子节点拆分 rank 与 rank_change；图片徽章排名按表格行序补齐
- 项目导出已升级：一个“导出项目”操作会在用户选择的位置创建 `CrawlHub项目/类目简称_日期/`，并写入 metadata.json、products.json、products.xlsx
- metadata 已改为采集当前页时自动生成，包含 page_title、url、created_at、category_full、category_short 和 rank_type；类目不存在时写入“未识别”并允许导出
- 面板兼容性已修复：新版脚本注入时自动重建旧版面板，避免继续执行旧的手动类目采样逻辑
- 扩展重复注入已修复：analyzer.js 使用独立作用域，popup 多次打开不会因顶层常量重复声明而报错
- 内部项目数据层已完成：采集结果会保存到扩展本地 storage，以“类目简称_日期”为项目键，包含 metadata 与标准化 products
- 内部商品字段已完善：保存 rank、rank_change、商品名称、图片、价格、评分、评价数量、GMV、点击、点击率、店铺和同款商品数；项目导出从已保存数据读取
- 商品情报中心第一版已完成：插件菜单新增工作台入口，优先读取当前项目、找不到时读取最近项目，不重新采集页面
- 商品情报中心界面已重构：采用左侧榜单导航、顶部榜单/类目/时间/筛选区和商品研究主体，接近 TikTok 商品榜布局
- 商品情报中心展示已完善：固定商品区与横向指标区拆分为独立容器；排名、排名变化、图片与商品名、价格范围、评分恒定可见，GMV、点击、点击率、店铺、同款和操作仅在右侧滚动，互不遮挡
- 商品情报中心信息密度已优化：固定区 460px、商品标题最多两行、所有商品行统一高度，普通窗口可同时浏览商品身份与前三项关键指标
- 商品情报中心扩展区已预留：每行提供 AI分析、找货源、收藏 UI 入口，暂不执行功能
- 数据采集字段已完善：结果按排名、商品名称、图片、价格范围、商品评分、评价数量、GMV、点击次数、点击率、店铺、同款商品数固定排序
- 数据采集输出已完善：新增本地 xlsx 导出，自动列宽、冻结首行并保留图片 URL
- 图片提取已优化：优先读取图片元素的 src、data-src 等属性，不从文本推断图片 URL
- 智能分页采集已完成：可读取当前范围、总数据量、每页数量并计算总页数
- 分页交互已调整：删除自动翻页和停止分页采集，改为用户切页后手动点击“采集当前页”
- 人工分页采集已完成：多次当前页结果按排名或通用记录键合并，重复采集不会重复计数
- 采集面板已优化：主操作为当前页采集，CSV/JSON/Excel 收纳到导出菜单，字段和结果区域支持内部滚动
- 采集状态已完善：显示采集中、当前完成页码、本页完成提示和累计已采集数量
- 采集任务管理已完善：支持清除已采集数据和当前进度，同时保留字段模板、页面分析和元素采样结果
- 采集任务管理已完善：检测分页数量、页面路径和列表结构变化，采集前提示是否开始新任务，避免旧任务数据混入
- 已使用 3 份 TikTok Shop 热卖榜分析报告作为测试依据；实现未依赖其中的 class 或 XPath

## 当前

- 页面分析、元素采样、数据采集保持可用；商品情报中心可读取已保存项目并以后台式商品榜浏览；等待 TikTok 页面登录后的真实验证

## 问题

- 无

## 下一步

- 在 TikTok 商品榜页面登录后刷新插件面板，采集至 200 条并进入商品情报中心，确认固定商品列、横向指标滚动、项目数据重读和三个导出文件
