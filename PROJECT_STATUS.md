# Project Status

## 已完成

- CrawlHub 保持纯浏览器数据采集定位：页面分析、元素采样、字段模板、当前页/人工多页累计采集、本地项目保存与 CSV/JSON/XLSX 项目导出均可用。
- 元素采样支持开始、暂停、恢复、取消和 ESC 退出；暂停时可正常滚动与操作复杂页面。
- TikTok 商品热卖榜已基于表头关系适配六类榜单：总榜、直播榜、短视频榜、商品卡、达人榜、新品榜；不依赖固定 class、XPath 或列位置。
- 通用商品字段保持不变；直播榜新增“直播账号”、短视频榜新增“表现最佳的视频”、达人榜新增“带货达人”，其他榜单对应字段为空。
- 项目 metadata 会保存当前榜单类型 `rank_type`；新增特殊字段同步写入本地项目、JSON、CSV 与 XLSX。
- 扩展重载回归已修复：重新打开插件面板时会替换失效的 content script 通信桥并重建旧面板；旧面板遇到失效上下文会给出恢复指引，不再显示底层英文错误。
- 导出管理已升级：齿轮可设置并保存默认导出根目录；导出结构改为“根目录 / 类目 / 榜单”，类目名称自动清洗且不含日期，重复榜单目录会先询问是否覆盖，并生成 metadata.json、products.json、products.xlsx 和 products.csv。
- 页面连接恢复已升级：新增顶部“重新连接页面”入口，采集前自动检查连接；检测到插件更新时自动刷新当前页并重新注入面板，已采集会话、字段模板和导出目录设置均保留。
- 采集任务已隔离：任务唯一键为“类目 + 榜单类型”；同榜单翻页继续追加，切换榜单会提示并保留旧任务；任务快照、字段模板、累计数量与暂停/结束状态写入本地项目和页面会话。
- 数据采集面板新增当前任务状态、暂停/继续采集和取消当前任务控件；取消只结束任务，不删除已有数据。
- 悬浮面板标题栏支持拖动；顶部设置、刷新、最小化、关闭按钮保持原有点击行为，拖动时禁止网页文字选中并显示可移动光标。
- 页面类型识别已增加 `product_rank` 与 `product_opportunity`：TikTok Shop 商品机会-热门关键词页面命中多个目标表头后切换为“开始、暂停、停止、导出、清除”界面；支持自动识别、字段提取、滚动容器分析、统一滚动引擎动态加载累计、滚动高度/位置稳定和底部多轮确认；以“关键词 + 类目”去重。商品机会在打开面板、开始或清除时都会重置为新会话，不恢复旧采集结果；热卖榜保持原有采集流程。
- 数据采集面板已统一商品机会与热卖榜的状态展示：顶部显示已识别页面、榜单/模式与采集状态，普通用户仅见固定的开始、暂停、停止、导出、清除操作；metadata、导出目录、字段来源与预览等调试信息收进默认折叠的“高级信息”。商品机会任务变化会直接切换并保留旧任务数据。
- 页面分析的 `structures.scroll_containers` 现会输出真实可滚动元素的 selector、scrollHeight、clientHeight、overflow 与列表包含标记；商品机会采集复用同一判定，以列表结构、有效可视高度、与目标表格的 DOM 距离和滚动高度优先选择容器，不依赖不稳定的 `contains_target` 标记或平台容器 class。
- 导出按数据来源拆分字段：商品机会的 JSON/XLSX/CSV 仅输出 keyword、category、source、search_count、selling_products，并使用“商品机会_热门关键词”文件名；热卖榜维持原有 `products.*` 文件和字段结构。
- 导出完成提示现在显示文件名、数量及“父文件夹 / 类目文件夹”的相对位置，不显示系统绝对路径。
- 已建立稳定基线：`stable-2026-09-15` 标签对应当前已推送代码，并重新生成 `dist/CrawlHub_稳定版_0.1.0.zip`，后续开发可在不改动该标签的前提下继续。
- 已新增“商品绑定”模块：独立 Tab 自动滚动商品机会列表，等待动态内容加载并按“关键词 + 类目”去重建立当前页面会话索引；定位关键词后滚动到目标行，打开操作菜单并点击“绑定现有商品”后停止。
- 已新增公共 `scanProductOpportunityScroll()` 滚动扫描引擎；商品机会采集和商品绑定扫描共用滚动容器、回到顶部、步进、动态等待、滚动高度/位置、新增数量及底部稳定判断，保留各自的数据提取和索引逻辑。
- 商品机会统一滚动扫描引擎已增加开发用 `console.debug` 诊断日志：记录每轮滚动位置、容器尺寸、底部判断、数量变化、滚动前后值和结束原因，不显示在用户界面。
- 商品机会滚动引擎已改为 400–800px 确定性动态步长，并通过 scrollTop/scrollHeight 短轮询稳定检测等待动态加载；正常内容稳定时快速继续，变化或延迟加载时延长观察，不改变结束判断原则。
- 商品绑定关键词索引与搜索共用标准化处理：去除首尾空格、去除开头 `#` 及其后空格并转为小写。

## 当前

- 六份真实页面采样报告已完成自查：公共字段结构一致，直播榜/短视频榜/达人榜的特殊字段分别映射到“直播账号/表现最佳的视频/带货达人”；表头匹配、排名与排名变化拆分、项目序列化、CSV/XLSX 导出已通过回归检查。
- 榜单 metadata 现在会检查激活标签的父级状态；排名列在页面只暴露趋势节点时按行序补主排名，避免把趋势数字写入 `rank`；短表头“商品”不会再误匹配“同款商品数”。
- 短视频榜和达人榜已增加可见关联对象采集：`related_content.video.items[]` 保存视频封面、可探测链接和数量提示；`related_content.creator.items[]` 保存达人头像、可探测主页、名称和数量提示；不影响原有 `best_video`、`creator` 商品字段。
- 关联对象已接入项目标准化与 CSV/XLSX 导出；嵌套对象以 JSON 字符串写入表格文件，避免导出为 `[object Object]`。
- 短视频榜已补充独立 `videos[]` 字段模板与列兜底识别：每个视频保存 `cover`、`creator`；只读取页面已展示的封面，不点击视频、不获取播放地址，缺失封面的卡片会跳过。
- 已生成可分享的 `0.1.0` Chrome/Edge 发布压缩包，并附带电脑小白上手指南；压缩包根目录可直接加载扩展。
- 已新增 `HANDOFF.md`，供新对话快速恢复项目范围、代码入口、数据结构、Git 状态和后续验证重点。
- 商品绑定模块不使用固定 XPath、固定 class 或固定按钮位置；关键词、类目、表格行列关系、滚动容器和动态菜单文字均在运行时读取，索引不写入永久存储。
- 商品绑定新增单条“自动提报（测试）”：在扫描完成后可按关键词和完整商品 ID 打开绑定入口、搜索并唯一核验结果、确认勾选、下一步及单次提交；每步均等待 DOM 状态，失败立即停止且不重试提交。
- 自动提报的商品搜索会确认商品 ID 已写入 `#search_content_input`，并在该输入组件内点击右侧搜索控件；目标商品每次最多等待 15 秒出现后才进入唯一匹配判断。
- 自动提报商品搜索已增加时序容错：首次 15 秒未出现目标时，仅在第 1 步窗口仍存在时补点一次搜索，不重新输入或无限重试；识别到明确空结果会立即停止。
- 自动提报第一步现会在当前抽屉内部确认可见“商品名称”表头和搜索框后立即进入搜索；不依赖原价、库存、默认商品行或固定等待，后续由 DOM 状态推进。
- 自动提报搜索现会按放大镜图标中心点识别实际命中元素并发送完整鼠标事件；点击前后比对商品行变化，提供“导出调试日志”用于排查点击未生效。
- 商品绑定批量自动提报已增强稳定性与可观察性：缓存关键词定位会在目标位置及有限 fallback 位置等待虚拟列表的滚动和可见关键词行连续稳定，再判定是否失效，不自动重扫；checkbox 异常时会额外确认目标行是否晚出现“已绑定”，避免误记失败。商品搜索找到唯一行后也会等待行节点、文本和 loading 状态稳定，再判断已绑定或勾选。批量调试日志区分 `last_business_step` 与 `last_event`，便于定位真实失败步骤。成功/已绑定结果会在页面恢复前锁定，确认抽屉关闭和商品机会“操作”表头恢复后固定冷却 2 秒再继续。业务结果 CSV 现含原始序号，批量区域可直接导出独立事件日志；第 1 步、搜索、第二步、提交成功和主表恢复的安全 timeout 已放宽至 20–30 秒。
- 自动提报 checkbox 在目标商品行稳定且确认未绑定后会额外等待 800ms，再按完整商品 ID 重新获取唯一行与 checkbox，并以中心点真实鼠标事件点击；首次未确认 checked 时，500ms 后重新获取并最多重试一次，同时保留已绑定二次确认。批量调试日志会记录缓冲、两次点击、checked 确认与重试结果。
- 自动提报现会在唯一目标商品行内识别可见“已绑定”：不勾选、不提交，按抽屉右上角关闭图标恢复主表后记为 `skipped`；批量统计与结果导出包含跳过数量，模板下载改为读取内置文件内容后在页面生成 Blob 下载。
- 商品绑定新增“调试模式（使用扫描缓存）”：开关和索引通过扩展 `chrome.storage.local` 按站点、`shop_region`、店铺标识保存；缓存恢复为 `source: "cache"` 会话并重新定位 DOM，关闭时清除当前范围缓存与开关状态。
- 商品绑定调试缓存的五项读写操作已由 content.js 直接使用 `chrome.storage.local`，错误状态会显示真实错误并输出开发日志。
- 商品绑定扫描缓存现保留当前快照、上一版快照和最近一次差异；以“标准化关键词 + 类目”比较新增、移除及线索来源/搜索次数/在售商品变化，定位字段继续只用于页面定位。
- 商品绑定面板会显示关键词库数量、与上一版的变化摘要和更新时间；首次可关联 CrawlHub 项目根目录，目录句柄复用默认导出目录的 IndexedDB structured-clone 保存机制。关联过程会显示 manifest/data/保存/权限/测试写入阶段及真实错误；扫描完成后会校验 snapshot 数量、重新查询写入权限，并对固定 `data/product_opportunity_keywords.json` 执行写入后回读验证，成功显示实际同步数量；浏览器不支持或目录授权被拒绝时保留 Blob 下载兜底。项目内同路径文件保持稳定 JSON 结构，供后续标题 Skill 读取。
- Skill 数据源同步新增当前浏览器会话内的专用事件日志 `window.__crawlHubSkillSyncLog`，商品绑定面板可导出 `crawlHub-skill-sync-时间.json`；日志只包含同步阶段、数量、目录名和固定目标文件，不写入仓库。
- 新增独立“商品采集”顶级 Tab：仅识别 `shop.tiktok.com/view/product/<数字 ID>` 且具有商品标题的前台商品详情页。素材包 V1 从当前 DOM 采集基础信息、顶部轮播、属性表与商品描述，并保存到 `data/tiktok_products/<product_id>/product.json` 及图片子目录；本轮不调用 SKU，不调用 TikTok API，不影响现有三项功能或既有导出结构。
- 商品采集复用已关联 CrawlHub 项目目录，支持通过扩展后台桥下载 TikTok 商品 CDN 图片；图片失败保留 `source_url` 并将 `local_path` 写为 `null`，`product.json` 最后写入并回读校验后才显示完成。新增 `window.__crawlHubProductDetailDebugLog` 与调试日志导出。
- 根据真实商品 `1729864263782341351` 的 22 张图片全部“未收到图片数据”日志，已将图片桥从 ArrayBuffer 改为分块 Base64；页面端解码后校验数据长度，文件写入后再次校验实际大小，并记录 `image_download_received`、`image_write_verified`。
- 商品素材 basic 字段已简化为 title、sold、seller、rating、review_count；不再采集或显示 price、currency、original_price、discount，也不调用 SKU 流程。
- 已新增独立“开启主图搜货”开关：开启后仅在 TikTok 商品详情页主图悬停显示入口，主图会提交给 1688 官方图搜；后台复用浏览器现有 1688 登录态和官方上传、搜索、DOM 补全顺序，支持裁剪重搜、登录/安全验证提示、任务恢复与取消，不写入商品素材包。
- 主图搜货已改为默认开启的全局悬浮入口：鼠标移入当前可见的顶部主图时实时识别并定位单一放大镜，切换轮播后读取当前图片；关闭 1688 结果面板只取消该任务，不会销毁悬浮入口。1688 首屏 8 张结果优先加载，直连超时或失败时按需通过限流 4 路后台图片代理兜底，后续图片进入结果滚动区后再加载。
- 已修复扩展重载后的旧 TikTok 内容脚本报错：runtime/storage 调用统一捕获 `Extension context invalidated`，旧 UI 和页面监听器只清理一次，并在 10 秒内最多自动刷新当前 TikTok 商品页一次；正常扩展上下文行为不变。
- TikTok → 1688 入口已支持顶部 Gallery 内所有可见商品图和缩略图：优先通过轮播计数及局部祖先识别 GalleryRoot，未及时识别时降级到顶部最大商品图；1688 结果卡图片区已与 meta 信息隔离，并按 `offer_id` 增量更新，enrichment 从 20 条扩展时不再重建已加载图片。
- 1688 结果图片加载已改为显式状态机：直连超时只启动后台代理，不再提前显示失败；直连或代理加载成功后终态锁定，后续错误不能覆盖；仅直连和代理都真实失败或最终超时才显示“图片暂不可用”。图片 URL 更新且旧图未成功时会清理状态并重试；前端与后台均保留 host、HTTP 状态、content-type、耗时和真实错误诊断，不记录图片数据、Cookie 或令牌。

## 问题

- 当前浏览器没有可用的已登录 TikTok Seller Center 页面，尚未完成六榜真实页面逐一采集、导出及扩展重载恢复验证，也未完成统一滚动引擎的真实长列表扫描和商品绑定定位打开验证；短视频与达人独立采样已确认视频封面和达人头像结构，但未提供视频播放地址、达人名称或主页 href/data-url。
- TikTok Shop 前台商品页在自动验证时出现安全检查，未绕过该页面；商品采集尚待用户可正常访问的真实商品页完成端到端验证。
- 最新真实测试确认目录和 `product.json` 正常，但 22 张图片均未落盘；本轮 Base64 修复尚待用户重载扩展后复测文件真实大小和可打开性。
- 1688 主图搜货尚未在真实 TikTok 商品页和已登录 1688 会话完成端到端验证；本轮图片状态机与代理错误日志已完成静态检查，仍需实机确认直连/代理竞态和失败显示；不会绕过 TikTok 安全检查、1688 登录或验证码。
- 本轮无法在未获得浏览器扩展重载操作确认的情况下执行实机验证；静态语法、清单、MD5 与 1688 图片 URL 规范化检查已完成。
- 上述扩展重载场景尚待用户实机复测自动刷新与主图搜货恢复。
- Gallery 轮播与 60 条结果卡的真实页面验收尚待用户重载扩展后复测；代码级语法和差异检查已完成。

## 下一步

- 登录 TikTok Seller Center 后，先验证统一滚动引擎能扫描到列表底部且数量持续增加；再用一条已加载关键词验证“定位并打开”完整流程。随后验证短视频榜采集与导出、六榜采集与导出及任务恢复。如需真实 URL 或达人名称，再分别采样视频卡片和达人头像容器的链接/文本属性。
- 商品绑定扫描新增暂停/继续控制：暂停时保留当前页面会话索引与已发现数量，继续时从当前滚动位置恢复，不清空、不回顶、不重启。
- 建议先关联真实 CrawlHub 项目根目录并完成一次扫描，确认固定 `data/product_opportunity_keywords.json` 自动更新；再扫描一次验证新增/移除/数据变化摘要与自动同步。批量提报仍建议先用 5–10 条任务验证缓存定位恢复、已绑定晚出现、主表恢复、2 秒冷却与结果/批量调试日志是否一致。
- 在可正常打开的 `shop.tiktok.com/view/product/...` 商品页重载扩展后，进入“商品采集”执行一次素材包保存；核对商品 ID、标题、价格、轮播/详情图片、本地路径、属性和描述文本，不测试 SKU。
- 重载扩展后复测商品 `1729864263782341351`：确认 `gallery/`、`description/` 图片真实存在且可打开，`product.json` 中成功图片的 `local_path` 与文件一致；不测试价格和 SKU。
- 重载扩展后，在可正常访问的 TikTok 商品页打开“商品采集”并开启“主图搜货”；验证悬浮入口、1688 登录恢复、裁剪重搜、结果卡片和关闭/折叠。确认关闭或禁用后入口立即消失，且商品素材包不新增 1688 数据。
- 重载扩展后实测主图搜货：首次 hover、主图到放大镜的鼠标移动、关闭结果面板后再次 hover、轮播切换、关闭/重新开启设置、首屏结果图和直连失败后的图片代理；当前 CrawlHub 停留任意 Tab 时均应可用。
- 重载扩展后重点验证 1688 结果图：直连成功不能被代理失败覆盖；直连失败后代理成功应显示图片；两者都失败或最终超时才显示“图片暂不可用”，并在控制台确认代理日志包含 host、status、content_type、elapsed_ms、error。
- 在 TikTok 商品页保持不刷新，重载 CrawlHub 扩展后点击旧面板关闭或触发搜货事件，确认页面只自动刷新一次且不再出现 `Extension context invalidated`。
- 重载扩展后验证主图、第 2/3 张缩略图、推荐图/详情图过滤；确认首屏 4 张图片、业务文字、enrichment 增量和后续滚动加载均正常。
