(() => {
  const ENABLED_KEY = "crawlHub.tiktokImageSearch.enabled.v1";
  const BUTTON_MARK = "crawlHub1688SearchButton";
  const PANEL_ID = "crawlHub-1688-sourcing";
  let enabled = false, observer, panelHost, panelShadow, currentJob, cropMode = false, cropSource = "", firstNotice = false;
  const hosts = new WeakMap();
  const clean = (value) => String(value || "").replace(/\s+/g, " ").trim();
  const escape = (value) => clean(value).replace(/[&<>'"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" })[character]);
  function productId() { return location.pathname.match(/^\/view\/product\/(\d{8,24})(?:\/|$)/)?.[1] || ""; }
  function isProductPage() { return location.hostname === "shop.tiktok.com" && Boolean(productId()); }
  function imageUrl(image) { return image?.currentSrc || image?.getAttribute("src") || ""; }
  function isSourceImage(url) { try { const parsed = new URL(url, location.href), target = `${parsed.hostname}${parsed.pathname}`.toLowerCase(); return /(?:tiktokcdn|ibytedtos|byteimg)/.test(parsed.hostname) && !/(?:logo|icon|avatar|static)/.test(target); } catch { return false; } }
  function isVisible(element) { const rect = element.getBoundingClientRect(), style = getComputedStyle(element); return style.display !== "none" && style.visibility !== "hidden" && Number(style.opacity || 1) > 0 && rect.width > 80 && rect.height > 80 && rect.bottom > 0 && rect.right > 0 && rect.top < innerHeight && rect.left < innerWidth; }
  function mainImage() {
    if (!isProductPage()) return undefined;
    const candidates = [...document.images].filter((image) => isVisible(image) && isSourceImage(imageUrl(image))).map((image) => ({ image, area: image.getBoundingClientRect().width * image.getBoundingClientRect().height, natural: image.naturalWidth * image.naturalHeight })).sort((a, b) => b.area - a.area || b.natural - a.natural);
    return candidates[0]?.image;
  }
  function currentProduct(image) {
    const id = productId(), url = imageUrl(image), title = clean(document.querySelector("h1")?.textContent || document.querySelector('[data-e2e*="title"]')?.textContent || document.title);
    return id && isSourceImage(url) ? { product_id: id, title: title || id, image_url: url } : undefined;
  }
  function installButton(image) {
    const wrapper = image?.parentElement;
    if (!wrapper || hosts.has(wrapper)) return;
    if (getComputedStyle(wrapper).position === "static") wrapper.style.position = "relative";
    const host = document.createElement("span"); host.dataset[BUTTON_MARK] = "true"; host.style.cssText = "position:absolute;right:12px;bottom:12px;z-index:3;display:none;pointer-events:auto;";
    const shadow = host.attachShadow({ mode: "open" });
    const style = document.createElement("style"); style.textContent = "button{display:grid;place-items:center;width:42px;height:42px;border-radius:50%;border:2px solid #ff0050;background:#fff;color:#e60046;box-shadow:0 3px 12px rgba(0,0,0,.28);cursor:pointer;padding:0}button:hover{background:#fff0f4}svg{width:21px;height:21px;fill:none;stroke:currentColor;stroke-width:2.2}";
    const button = document.createElement("button"); button.type = "button"; button.title = "搜索1688同款货源"; button.setAttribute("aria-label", "搜索1688同款货源"); button.innerHTML = '<svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="10.8" cy="10.8" r="6.8"></circle><path d="m16 16 5 5"></path></svg>';
    button.addEventListener("click", (event) => { event.preventDefault(); event.stopPropagation(); const product = currentProduct(image); if (product) void start(product); });
    shadow.append(style, button); wrapper.appendChild(host); hosts.set(wrapper, host);
    const show = () => { if (enabled) host.style.display = "block"; }, hide = () => { if (!host.matches(":focus-within")) host.style.display = "none"; };
    wrapper.addEventListener("mouseenter", show); wrapper.addEventListener("mouseleave", hide); wrapper.addEventListener("focusin", show); wrapper.addEventListener("focusout", hide);
  }
  function scan() { if (!enabled || !isProductPage()) return; const image = mainImage(); if (image) installButton(image); }
  function removeUi(cancel = false) { observer?.disconnect(); observer = undefined; for (const host of document.querySelectorAll("[data-crawl-hub1688-search-button]")) host.remove(); panelHost?.remove(); panelHost = panelShadow = undefined; cropMode = false; if (cancel) void chrome.runtime.sendMessage({ type: "CANCEL_1688_IMAGE_SEARCH" }).catch(() => undefined); }
  function enable() { if (enabled || !isProductPage()) return; enabled = true; scan(); observer = new MutationObserver(scan); observer.observe(document.documentElement, { childList: true, subtree: true, attributes: true, attributeFilter: ["src", "srcset"] }); }
  function disable() { enabled = false; removeUi(true); }
  function stateText(job) { if (!job) return "选择商品主图开始搜货"; if (job.state === "preparing-image") return "正在准备图片…"; if (job.state === "uploading") return "正在提交1688官方图搜…"; if (job.state === "searching" || job.enrichment_state === "running") return "正在匹配同款货源…"; if (job.state === "ready") return job.results?.length ? `找到 ${job.results.length} 条1688货源` : "1688本次没有返回货源"; if (job.state === "needs-auth") return job.auth_issue === "challenge" ? "1688暂时限制访问" : "需要连接1688"; return job.state === "cancelled" ? "搜索已取消" : "搜索失败"; }
  function ensurePanel() { if (panelHost?.isConnected) return; panelHost = document.createElement("aside"); panelHost.id = PANEL_ID; panelHost.style.cssText = "position:fixed;left:0;top:0;bottom:0;z-index:2147483644;"; panelShadow = panelHost.attachShadow({ mode: "open" }); document.documentElement.appendChild(panelHost); }
  async function start(product, dataUrl) { ensurePanel(); cropMode = false; const stored = await chrome.storage.local.get("crawlHub.1688ImageSearchNotice.v1").catch(() => ({})); firstNotice = !stored["crawlHub.1688ImageSearchNotice.v1"]; if (firstNotice) await chrome.storage.local.set({ "crawlHub.1688ImageSearchNotice.v1": true }).catch(() => undefined); currentJob = { source_product_id: product.product_id, source_title: product.title, source_image_url: product.image_url, query_image_data_url: dataUrl || "", state: "preparing-image", results: [] }; render(); const response = await chrome.runtime.sendMessage({ type: "START_1688_IMAGE_SEARCH", product, query_image_data_url: dataUrl }).catch((error) => ({ ok: false, error: String(error) })); if (!response?.ok && currentJob) { currentJob.state = "failed"; currentJob.error = response?.error || "无法启动1688图搜。"; render(); } }
  function card(result, index) { return `<article class="card" data-offer="${escape(result.offer_id)}" tabindex="0" role="link"><i>${index + 1}</i><div class="pic">${result.image_url ? `<img src="${escape(result.image_url)}" alt="" loading="lazy">` : "图片未返回"}</div><h3>${escape(result.title)}</h3><b class="price">${escape(result.price?.display || "—")}</b><div class="facts"><span>销量 ${escape(result.sales || "—")}</span><span>起批 ${escape(result.minimum_order || "—")}</span><span>回头率 ${escape(result.repurchase_rate || "—")}</span>${result.shipping_time ? `<span>发货 ${escape(result.shipping_time)}</span>` : ""}${result.supplier_years ? `<span>店龄 ${escape(result.supplier_years)}</span>` : ""}</div>${result.badges?.length ? `<div class="badges">${result.badges.map((item) => `<em>${escape(item)}</em>`).join("")}</div>` : ""}<footer>${escape(result.supplier_name || "供应商未显示")} <b>↗</b></footer></article>`; }
  const style = `:host{all:initial}*{box-sizing:border-box}.panel{width:min(820px,calc(100vw - 54px));height:100vh;background:#292929;color:#f5f5f5;box-shadow:16px 0 42px #0006;font:14px/1.45 -apple-system,BlinkMacSystemFont,"Segoe UI","Microsoft YaHei",sans-serif;display:flex;flex-direction:column}.head{min-height:61px;display:flex;align-items:center;gap:14px;padding:0 15px;background:#303030;border-bottom:1px solid #464646}.brand{color:#ff5a83;font-size:18px;font-weight:800}.state{color:#c4c9c7;font-size:12px}.actions{margin-left:auto;display:flex;gap:7px}.icon{width:33px;height:33px;border:1px solid #555;border-radius:7px;background:#373737;color:#fff;cursor:pointer}.body{display:flex;min-height:0;flex:1}.side{width:205px;padding:13px;background:#2e2e2e;border-right:1px solid #464646;overflow:auto}.query{width:100%;height:170px;object-fit:contain;background:#fff;border-radius:5px}.title{margin:10px 0 3px;font-size:12px}.id{font-size:11px;color:#aaa}.btn{width:100%;margin-top:9px;padding:9px;border:1px solid #666;border-radius:6px;background:#3d3d3d;color:#fff;cursor:pointer;font-weight:700}.btn.primary{background:#e60046;border-color:#e60046}.notice{margin-top:12px;padding:9px;background:#443138;color:#ffd0dc;border-radius:6px;font-size:11px}.results{flex:1;min-width:0;display:flex;flex-direction:column}.toolbar{padding:12px 15px;border-bottom:1px solid #464646;color:#ff9ab5}.scroll{overflow:auto;padding:14px;flex:1}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.card{position:relative;padding:9px;background:#353535;border:1px solid #4a4a4a;border-radius:7px;cursor:pointer}.card:hover{border-color:#ff5a83}.card>i{position:absolute;z-index:2;top:14px;left:14px;background:#fff;color:#555;width:23px;height:23px;border-radius:50%;text-align:center;font-style:normal}.pic{height:160px;display:grid;place-items:center;background:#fff;color:#777;font-size:11px}.pic img{width:100%;height:100%;object-fit:contain}.card h3{height:38px;overflow:hidden;margin:8px 0 5px;font-size:12px;line-height:19px}.price{color:#ff7197}.facts{display:grid;grid-template-columns:1fr 1fr;gap:3px;margin-top:7px;color:#b8bebc;font-size:10px}.badges{display:flex;gap:3px;overflow:hidden;margin-top:6px}.badges em{font-style:normal;background:#523840;color:#ffb5ca;padding:1px 4px;border-radius:3px;font-size:10px;white-space:nowrap}.card footer{margin-top:7px;padding-top:6px;border-top:1px solid #484848;color:#bbb;font-size:10px;display:flex}.card footer b{margin-left:auto;color:#ff7297}.empty,.error,.loading{max-width:480px;margin:45px auto;text-align:center;padding:22px;color:#c4c9c7}.error{border:1px solid #92536a;background:#443138;border-radius:8px;color:#ffd2de}.error button{display:block;width:100%;margin-top:9px;padding:9px;border:0;border-radius:6px;background:#e60046;color:#fff;cursor:pointer}.error button.alt{background:#5a454c}.spinner{width:36px;height:36px;margin:0 auto 12px;border:4px solid #555;border-top-color:#ff5a83;border-radius:50%;animation:spin .8s linear infinite}.crop canvas{display:block;width:100%;background:#fff;touch-action:none}.cropbox{position:absolute;display:none;border:2px solid #ff5a83;background:#ff5a8333;pointer-events:none}.canvas{position:relative}.collapsed{position:fixed;top:42%;left:0;border:0;border-radius:0 8px 8px 0;background:#e60046;color:#fff;padding:12px 9px;writing-mode:vertical-rl;cursor:pointer}@keyframes spin{to{transform:rotate(360deg)}}@media(max-width:650px){.side{width:160px}.query{height:130px}.grid{grid-template-columns:1fr}.panel{width:calc(100vw - 24px)}}`;
  function render() { if (!panelShadow) return; const job = currentJob, results = job?.results || [], image = job?.query_image_data_url || job?.source_image_url || "", working = job && (["preparing-image", "uploading", "searching"].includes(job.state) || job.enrichment_state === "running"), auth = job?.state === "needs-auth", challenge = job?.auth_issue === "challenge", failed = job?.state === "failed"; panelShadow.innerHTML = `<style>${style}</style><section class="panel"><header class="head"><b class="brand">CrawlHub <small>1688官方图搜</small></b><span class="state">${escape(stateText(job))}</span><div class="actions"><button class="icon" data-collapse title="折叠">‹</button><button class="icon" data-close title="关闭">×</button></div></header><div class="body"><aside class="side">${cropMode ? '<div class="crop"><div class="canvas"><canvas></canvas><div class="cropbox"></div></div><p>拖动框选商品主体</p><button class="btn primary" data-crop-apply>用框选区域重搜</button><button class="btn" data-crop-cancel>取消</button></div>' : `${image ? `<img class="query" src="${escape(image)}" alt="搜索图片">` : ""}<div class="title">${escape(job?.source_title || "尚未选择商品")}</div><div class="id">商品 ID：${escape(job?.source_product_id || "")}</div><button class="btn" data-crop ${!image || working ? "disabled" : ""}>框选主体</button><button class="btn primary" data-retry ${!job || working ? "disabled" : ""}>重新搜索</button>${firstNotice ? '<div class="notice">当前商品图片会提交给1688官方图搜，用于查找相似货源。</div>' : ""}`}</aside><main class="results"><div class="toolbar">1688 推荐顺序 ${results.length ? `· ${results.length} 条结果` : ""}</div><div class="scroll">${auth ? `<div class="error"><b>${challenge ? "1688暂时限制访问" : "需要连接1688"}</b><p>${challenge ? "请在已有的1688页面完成验证，插件不会绕过验证。" : "请在当前浏览器完成1688登录，完成后会自动继续本次搜图。"}</p><button data-recheck>${challenge ? "我已完成验证，重新检测" : "我已登录，重新检测"}</button><button class="alt" data-login>${challenge ? "打开已有1688页面" : "连接1688"}</button></div>` : failed ? `<div class="error">${escape(job.error || "1688图搜暂时失败。")}<button data-official>打开1688官方搜图页</button></div>` : working ? `<div class="loading"><div class="spinner"></div>${escape(stateText(job))}</div>` : results.length ? `<div class="grid">${results.map(card).join("")}</div>` : `<div class="empty">${escape(job?.error || "1688本次没有返回相似货源，可以框选商品主体后重新搜索。")}</div>`}</div></main></div></section>`; bind(); if (cropMode) void initializeCrop(); }
  function bind() { panelShadow.querySelector("[data-close]")?.addEventListener("click", () => removeUi(true)); panelShadow.querySelector("[data-collapse]")?.addEventListener("click", () => { panelShadow.innerHTML = `<style>${style}</style><button class="collapsed">1688货源 ›</button>`; panelShadow.querySelector("button")?.addEventListener("click", render); }); panelShadow.querySelector("[data-retry]")?.addEventListener("click", () => { if (currentJob) void start({ product_id: currentJob.source_product_id, title: currentJob.source_title, image_url: currentJob.source_image_url }, currentJob.query_image_data_url || undefined); }); panelShadow.querySelector("[data-crop]")?.addEventListener("click", () => void openCrop()); panelShadow.querySelector("[data-crop-cancel]")?.addEventListener("click", () => { cropMode = false; render(); }); panelShadow.querySelector("[data-recheck]")?.addEventListener("click", () => void chrome.runtime.sendMessage({ type: "RECHECK_1688_LOGIN" })); panelShadow.querySelector("[data-login]")?.addEventListener("click", () => void chrome.runtime.sendMessage({ type: "REOPEN_1688_LOGIN" })); panelShadow.querySelector("[data-official]")?.addEventListener("click", () => void chrome.runtime.sendMessage({ type: "OPEN_1688_OFFICIAL_SEARCH" })); for (const element of panelShadow.querySelectorAll(".card")) { const open = () => void chrome.runtime.sendMessage({ type: "OPEN_1688_OFFER", offer_id: element.dataset.offer }); element.addEventListener("click", open); element.addEventListener("keydown", (event) => { if (event.key === "Enter") open(); }); } }
  async function openCrop() { if (!currentJob) return; cropSource = currentJob.query_image_data_url; if (!cropSource) { const response = await chrome.runtime.sendMessage({ type: "PREPARE_SOURCE_IMAGE", image_url: currentJob.source_image_url }); if (!response?.ok) { currentJob.state = "failed"; currentJob.error = response?.error || "无法读取原图。"; render(); return; } cropSource = response.data_url; } cropMode = true; render(); }
  async function initializeCrop() { const canvas = panelShadow.querySelector("canvas"), box = panelShadow.querySelector(".cropbox"); if (!canvas || !box || !cropSource) return; const image = new Image(); image.src = cropSource; await image.decode().catch(() => undefined); canvas.width = image.naturalWidth || image.width; canvas.height = image.naturalHeight || image.height; canvas.getContext("2d")?.drawImage(image, 0, 0, canvas.width, canvas.height); let sx = 0, sy = 0, ex = 0, ey = 0, dragging = false; const point = (event) => { const rect = canvas.getBoundingClientRect(); return { x: Math.max(0, Math.min(rect.width, event.clientX - rect.left)), y: Math.max(0, Math.min(rect.height, event.clientY - rect.top)) }; }, paint = () => { box.style.display = "block"; box.style.left = `${Math.min(sx, ex)}px`; box.style.top = `${Math.min(sy, ey)}px`; box.style.width = `${Math.abs(ex - sx)}px`; box.style.height = `${Math.abs(ey - sy)}px`; }; canvas.addEventListener("pointerdown", (event) => { const pointValue = point(event); sx = ex = pointValue.x; sy = ey = pointValue.y; dragging = true; canvas.setPointerCapture(event.pointerId); paint(); }); canvas.addEventListener("pointermove", (event) => { if (!dragging) return; const pointValue = point(event); ex = pointValue.x; ey = pointValue.y; paint(); }); canvas.addEventListener("pointerup", (event) => { dragging = false; canvas.releasePointerCapture(event.pointerId); }); panelShadow.querySelector("[data-crop-apply]")?.addEventListener("click", () => { const rect = canvas.getBoundingClientRect(), x = Math.min(sx, ex) * canvas.width / rect.width, y = Math.min(sy, ey) * canvas.height / rect.height, width = Math.abs(ex - sx) * canvas.width / rect.width, height = Math.abs(ey - sy) * canvas.height / rect.height; if (width < 20 || height < 20 || !currentJob) return; const output = document.createElement("canvas"); output.width = Math.round(width); output.height = Math.round(height); output.getContext("2d")?.drawImage(canvas, x, y, width, height, 0, 0, output.width, output.height); void start({ product_id: currentJob.source_product_id, title: currentJob.source_title, image_url: currentJob.source_image_url }, output.toDataURL("image/jpeg", .88)); }); }
  chrome.runtime.onMessage.addListener((message) => { if (message?.type === "SOURCING_JOB_UPDATED" && message.job) { currentJob = message.job; if (currentJob.state !== "cancelled") { ensurePanel(); render(); } } });
  chrome.storage.onChanged.addListener((changes, area) => { if (area === "local" && changes[ENABLED_KEY]) changes[ENABLED_KEY].newValue ? enable() : disable(); });
  if (!globalThis.__crawlHubTikTokImageSearchInstalled && isProductPage()) { globalThis.__crawlHubTikTokImageSearchInstalled = true; chrome.storage.local.get(ENABLED_KEY).then((stored) => { if (stored[ENABLED_KEY]) enable(); }); chrome.runtime.sendMessage({ type: "GET_1688_IMAGE_SEARCH_JOB" }).then((response) => { if (response?.job && response.job.state !== "cancelled") { currentJob = response.job; ensurePanel(); render(); } }).catch(() => undefined); }
})();
